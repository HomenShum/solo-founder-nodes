{
  "name": "solo-founder-local-substrate",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Turnkey local-first reference of the Solo Founder Nodes substrates: SoloMemory (audit-safe memory) + SoloLedger (honest-lane eval ledger that DERIVES the gate, not accepts it). One command: npm i && npm run smoke.",
  "scripts": {
    "smoke": "tsx smoke.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "nanoid": "^5.0.9",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
