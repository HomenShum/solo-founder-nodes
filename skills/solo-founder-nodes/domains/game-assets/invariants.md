# Game Assets Domain Pack

Jobs:

- create engine-usable meshes,
- prove pivot, scale, materials, collision, LOD, import, and runtime budget,
- export to target engine workflows.

Blocking gates:

- engine-import proof,
- collision/LOD proof,
- runtime-budget proof.

A pretty model with no collision, LOD, correct pivot, or engine import proof is not game-ready.
